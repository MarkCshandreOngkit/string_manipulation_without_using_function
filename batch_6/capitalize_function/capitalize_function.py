#input string
string = input("Input: ")
#iterate through each character
for character in string:
    print(character)
    #check if character is the first
        #convert to upper
    #else
        #convert to lower
#print output